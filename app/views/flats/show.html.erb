<div class="container">
  <div class="row">
    <div class="col-xs-8 col-xs-offset-2">
      <div class="box">
      <h2><%= @flat.name %></h2>
      <p><%= @flat.address %></p>
      <p> <%= @flat.bookings.count %> bookings on this flat already </p>
      <%= link_to "book this flat", new_flat_booking_path(@flat, @booking) %>
      </div>
    </div>
  </div>
</div>
